# Use the public npm registry for all packages
registry=https://registry.npmjs.org/

# Prevent npm from using the Salesforce internal scope and registry
@sfdc-internal:registry=invalid://invalid.registry/